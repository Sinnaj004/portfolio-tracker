services:
  # --- Datenbank: TimescaleDB ---
  db:
    image: timescale/timescaledb:latest-pg16
    container_name: portfolio_db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"

  # --- Cache: Redis ---
  redis:
    image: redis:alpine
    container_name: portfolio_redis
    restart: always
    ports:
      - "6379:6379"

  # --- Backend: FastAPI ---
  backend:
    build: ./backend
    container_name: portfolio_backend
    restart: always
    depends_on:
      - db
      - redis
    env_file:
      - .env
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    # host 0.0.0.0 ist hier bereits korrekt für Netzwerkzugriff
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  # --- Frontend: React (Vite) ---
  frontend:
    build: ./portfolio-frontend
    container_name: portfolio_frontend
    ports:
      - "5173:5173"
    volumes:
      - ./portfolio-frontend:/app
      - /app/node_modules
    environment:
      # WICHTIG: Ersetze 192.168.xxx.xxx mit der IP deines Rechners!
      - VITE_API_URL=http://192.168.178.42:8000/api/v1
    depends_on:
      - backend

    command: npm run dev -- --host 0.0.0.0

volumes:
  db_data: